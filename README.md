# Human-Action-Recognition
A Human Action Recognition system developed using Python, MediaPipe, OpenCV, and SVM. It detects human poses, extracts body landmarks, and recognizes actions in real time using a webcam through a complete machine learning pipeline.
